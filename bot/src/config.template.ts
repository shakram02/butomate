import { BotConfig } from './bot_config';

// TODO: edit the fields here and rename the file to config.ts

export const config: BotConfig = {
  appId: 'appId',   // Microsoft app id
  appPassword: 'password',  // Microsoft app password
  pageKiteHandle: '***.pagekite.me', // Your handle
  portNumber: 8000  // Port number, don't use port number less than 1025
};
